{
    "private": true,
    "scripts": {
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "reset": "git reset Head~ --soft",
        "up": "cargo publish --allow-dirty"
    }
}
